# Trade Executor

Автоматический модуль копирования сделок с Hyperliquid. Мониторит таблицу `new_positions` в PostgreSQL и открывает позиции на вашем аккаунте.

## 🔒 Безопасность

- **Приватный ключ НИКОГДА не коммитится в Git**
- Все секреты хранятся в переменных окружения Railway
- `.env` файл добавлен в `.gitignore`
- Логи не содержат приватных ключей

## 📋 Переменные окружения

Настройте в Railway Variables:

| Переменная                | Описание                                              | Пример                        |
| ------------------------- | ----------------------------------------------------- | ----------------------------- |
| `DATABASE_URL`            | Строка подключения PostgreSQL (Railway автоматически) | `postgresql://...`            |
| `HYPERLIQUID_API_URL`     | URL API Hyperliquid                                   | `https://api.hyperliquid.xyz` |
| `HYPERLIQUID_PRIVATE_KEY` | **Приватный ключ кошелька (БЕЗ 0x)**                  | `abc123...`                   |
| `WALLET_ADDRESS`          | Адрес вашего кошелька                                 | `0x...`                       |
| `POSITION_SIZE_PERCENT`   | % от баланса для каждой сделки                        | `5.0`                         |
| `TELEGRAM_BOT_TOKEN`      | Токен Telegram бота                                   | `123456:ABC...`               |
| `TELEGRAM_CHAT_ID`        | ID чата для уведомлений                               | `123456789`                   |
| `POLL_INTERVAL_SECONDS`   | Интервал проверки БД (сек)                            | `5`                           |
| `HTTP_TIMEOUT_SECONDS`    | Таймаут HTTP запросов                                 | `10`                          |

## 🚀 Запуск

### Локально

```bash
cd trade_executor
python -m venv venv
source venv/bin/activate  # Windows: venv\Scripts\activate
pip install -r requirements.txt

# Создайте .env с переменными
python -m trade_executor.cli
```

### Railway

1. Подключите репозиторий https://github.com/spimethod/trade
2. Добавьте PostgreSQL сервис (если еще нет)
3. Настройте все переменные окружения в Variables
4. Start Command: `python -m trade_executor.cli`
5. Деплой автоматически запустится

## 📊 Как работает

1. Каждые 5 секунд (настраивается) проверяет таблицу `new_positions`
2. Для каждой новой записи:
   - Проверяет, открыта ли уже такая позиция (coin + side)
   - Если нет — рассчитывает размер позиции (% от баланса)
   - Открывает позицию с указанным плечом
   - Если не получается — пробует с меньшим плечом (20x → 10x → 5x → 3x → 1x)
   - Отправляет уведомление в Telegram
   - Удаляет запись из `new_positions`

## 🔐 Как получить приватный ключ

⚠️ **ВАЖНО:** Используйте отдельный кошелёк для торговли, не храните там все средства!

1. В MetaMask: Settings → Security & Privacy → Reveal Secret Recovery Phrase
2. Или экспортируйте приватный ключ конкретного аккаунта
3. Добавьте в Railway Variables **БЕЗ префикса 0x**

## 📱 Настройка Telegram

1. Создайте бота через @BotFather
2. Получите токен (формат: `123456:ABC-DEF...`)
3. Узнайте ваш chat_id:
   - Напишите боту любое сообщение
   - Откройте: `https://api.telegram.org/bot<ВАШ_ТОКЕН>/getUpdates`
   - Найдите `"chat":{"id":123456789}`
4. Добавьте оба значения в Railway Variables

## 📁 Структура проекта

```
trade_executor/
├── config/          # Загрузка настроек из окружения
├── database/        # Работа с PostgreSQL
├── hyperliquid/     # API Hyperliquid (состояние, ордера)
├── telegram/        # Уведомления в Telegram
├── positions/       # Логика проверки позиций
├── utils/           # Логирование
├── main.py          # Основной цикл
└── cli.py           # Точка входа
```

## ⚠️ Риски

- Торговля с реальными деньгами — всегда есть риск потери средств
- Проверьте настройки на testnet перед продакшеном
- Используйте разумный % от баланса (рекомендуется 3-5%)
- Следите за размером плеча

## 📝 Логи

Все действия логируются в stdout:

- Найденные новые позиции
- Проверка существующих позиций
- Открытие ордеров
- Ошибки и успехи
- Отправка уведомлений

## 🛠️ Troubleshooting

**Ошибка подключения к БД:**

- Проверьте, что `DATABASE_URL` корректен
- Убедитесь, что таблица `new_positions` существует

**Ошибка открытия позиции:**

- Проверьте баланс аккаунта
- Убедитесь, что приватный ключ корректен
- Проверьте доступность Hyperliquid API

**Не приходят уведомления:**

- Проверьте токен бота и chat_id
- Убедитесь, что написали боту хотя бы одно сообщение

## 📄 Лицензия

Проект для личного использования. Используйте на свой риск.
